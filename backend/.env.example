PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/portfolio
CLIENT_URL=http://localhost:5173
JWT_SECRET=change-this-to-a-long-random-string-in-production
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=choose-a-strong-password

# Optional: contact form → email (Gmail, Outlook, SendGrid SMTP, etc.)
# If SMTP is incomplete, the form still saves to MongoDB; only the email step is skipped.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=jobankang.work@gmail.com
SMTP_PASS=ybiu yirl ubtz kghz
# Where notifications are delivered (defaults to SMTP_USER if omitted)
CONTACT_NOTIFY_EMAIL=jobankang.work@gmail.com
# Optional From header (defaults to "Portfolio contact" <SMTP_USER>)
# MAIL_FROM="Portfolio" <your-email@gmail.com>
